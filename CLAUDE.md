# Rooted Outbound Web — Diego Vallota

> **Personal context**: Read `~/.claude/CLAUDE.md` and `~/.claude/projects/-Users-diegovallota/memory/user_diego_brain.md` for Diego's full background, story, and personality.

## Rules
- Never use em dashes in any content
- "Make it live" = commit + push to main (triggers Vercel deploy). Do this without asking.
- **STOP: DO NOT TOUCH THE BLUEPRINT COPY. This has been overwritten 4 times and it is a hard blocker.**
  - Files: `public/next-season-sports-activation-blueprint/index.html` and `thank-you/index.html`.
  - The written words are LOCKED in Rooted's RFP voice (see the `rooted-voice` skill). That means every: hero headline + subhead, the "Proof in the work" subline, all 9 case-study paragraphs + their "The goals:" bullet lists, the "How we think / How we actually plan an activation" playbook section, the "It lives on / Builds come down" section, the "What you get" cards, the fit/process/booking copy, and the thank-you copy.
  - You MAY edit: images, `<img src>`/`alt`, video embeds (`data-vimeo`), layout/markup structure, `styles.css`, and add NEW sections. You MAY NOT reword, replace, shorten, "improve", or regenerate any existing copy listed above.
  - NEVER rebuild `index.html` from scratch. It destroys the copy. Edit the existing file in place.
  - Before editing this page, run `git pull` so you are on the latest copy. After editing, run `git diff` and confirm you changed ZERO words of body/headline copy.
  - If you believe copy must change, STOP and ask Diego first. Do not change it on your own.
