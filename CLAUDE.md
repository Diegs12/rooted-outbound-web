# Rooted Outbound Web — Diego Vallota

> **Personal context**: Read `~/.claude/CLAUDE.md` and `~/.claude/projects/-Users-diegovallota/memory/user_diego_brain.md` for Diego's full background, story, and personality.

## Rules
- Never use em dashes in any content
- "Make it live" = commit + push to main (triggers Vercel deploy). Do this without asking.
- **Do NOT overwrite or regenerate the blueprint COPY unless explicitly told to.** The text in `public/next-season-sports-activation-blueprint/index.html` and `thank-you/index.html` is written in Rooted's locked RFP voice (see the `rooted-voice` skill). When editing this page for images, layout, video embeds, or styling, change ONLY those elements and leave the headlines, subheads, case-study paragraphs, GOALS lists, and card copy exactly as they are. If a rebuild is unavoidable, pull the latest committed copy first so it is not stomped.
